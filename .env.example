# Copy to .env and set your key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: restrict CORS to specific origins (default: allow any origin so anyone can access).
# Example: CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
# CORS_ORIGINS=
